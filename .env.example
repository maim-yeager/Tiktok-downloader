# ── TikTok Downloader API — Environment Variables ──────────────────────────────

# Server port (Render injects PORT automatically)
PORT=3000

# API key — change this to a strong secret before deploying!
API_KEY=maim1234
